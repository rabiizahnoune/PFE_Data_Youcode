cluster_name: 'FintechCluster'
num_tokens: 256
listen_address: cassandra
rpc_address: cassandra
seed_provider:
     - class_name: org.apache.cassandra.locator.SimpleSeedProvider
       parameters:
           - seeds: "cassandra"
commitlog_sync: periodic
commitlog_sync_period_in_ms: 10000